## SquareFreeQ

```
SquareFreeQ(n)
```

> returns `True` if `n` a square free integer number or a square free univariate polynomial.

### Examples
```
>> SquareFreeQ(9)
False

>> SquareFreeQ(105)
True
```